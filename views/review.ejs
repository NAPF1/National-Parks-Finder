<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <title>Leave a Review! </title>
</head>
<body class="d-flex flex-column min-vh-100">
	<%- include("partials/header.ejs") %>
	<%- include("partials/nav.ejs") %>

    
	<div class="card text-white bg-dark mb-3 text-center card w-50 my-auto">
  	<div class="card-body">
    	<h2 class="card-title">Leave a Review <br> for <br>
			<%=park[0].fullName%>
			</h2>
			<br>
			
    	<form method="POST" action="/review">
        <input type="hidden" name="username" value="<%= user[0].username %>">
        <input type="hidden" name="parkCode" value="<%= park[0].parkCode %>">
				<input type="hidden" name="parkName" value="<%= park[0].fullName %>">
        <input type="hidden" name="userId" value="<%= user[0].userId %>">
				
				<div class="form-group">
			    <label for="rating">Rating</label>
			    <select class="form-control" name="rating">
			     	<option value="1">1</option>
	          <option value="2">2</option>
	          <option value="3">3</option>
	          <option value="4">4</option>
	          <option value="5">5</option>
			    </select>
			  </div>
				
				<br>
				
				<div class="form-group">
					<label for="review">Review</label>
					<textarea class="form-control" name="review" rows="3"></textarea>
				</div>
				
				<br>

        <button class="btn btn-primary">Submit Review</button>
			</form>
  	</div>
	</div>
    
  
	<%- include("partials/footer.ejs") %>
  <script src="/js/login.js"></script>
</body>
</html>